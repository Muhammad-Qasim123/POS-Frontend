import React from 'react';
import SideBar from '../components/defaultLayout';

const HomePage = () => {
    return (
        <div>
            <SideBar/>
        
        </div>
    );
};

export default HomePage;